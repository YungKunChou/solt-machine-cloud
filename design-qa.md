# 批次設定 UI 驗證 — 2026-09-24

> 以下是初版歷史驗證紀錄。後續已依使用者要求統一兩區寬度、放大欄位文字並移除 ROG 風格；這些版面修改改由使用者人工確認，以下截圖與通過結果不代表目前版本。

final result: passed

## 比較目標與證據

- Source visual truth：`C:/Users/YKC/.codex/generated_images/01a0d166-f646-7ca2-9151-5cf684b4374c/exec-40fb6b21-7556-49cf-941d-31536adc2761.png`，使用者選定第 2 款。
- 本機頁面：`http://127.0.0.1:8080/` → 建立房間 → 編輯設定。
- 實作桌面截圖：`C:/tmp/Lottery 雙滾輪雲端版/.local-setup/qa/batch-editor-desktop.jpg`。
- 手機截圖：同目錄 `batch-editor-mobile-top.jpg`、`batch-editor-mobile-bottom.jpg`。
- 深色截圖：同目錄 `batch-editor-dark.jpg`。
- 桌面 CSS viewport：1444 × 1050；面板 CSS 寬 1380、高約 758；來源圖 1701 × 925，預期面板約 1380 × 750。
- 瀏覽器工具輸出的桌面點陣圖為 1429 × 969，手機為 375 × 812；手機 CSS viewport 為 390 × 844，回報 devicePixelRatio 約 1。工具截圖與設定 viewport 尺寸不同，因此以面板區域相對比例比對，沒有宣稱逐像素一致。
- 桌面比對狀態：淺色、主持人、編輯中、5 筆獎項／3 筆數量、獎項第 1 與第 3 筆勾選、第 3 筆聚焦、尚未儲存。測試用尾端空白未套用，已取消。
- Full-view evidence：在同一個比較輸入中同時顯示來源圖片與更新後瀏覽器截圖；實作截圖包含既有頁首，比對範圍限於設定面板。
- Focused evidence：完整比較中標題、兩欄工具列、各列輸入及底部操作均清楚可讀，無需額外放大區域；手機分別截取頂部獎項與下方數量／儲存列。

## Findings 與修正歷程

1. [P2，已修正] 初版面板偏緊，高約 652，未呈現來源圖的舒展行距。調整標題至 30px、輸入至 19px、列高至 72px、表頭至 56px，增加標題及頁尾間距；最終面板高約 758，兩欄比例及底部區域與來源方向一致。更新後證據為桌面截圖。
2. [P2，已修正] 原色主按鈕白字對比約 4.07:1，行號也偏淡。將主色改為 #82705e、次要文字改 #686c72，仍保留灰棕色方向；勾選框擴為 24px。更新後桌面及手機截圖可讀。
3. [P2，已修正] 390px 寬時既有頁首音樂按鈕覆蓋主題按鈕。為頁首工具列增加手機換行，音樂操作獨立下一行。手機頂部截圖確認無遮蓋；文件寬 375 小於 viewport 390，沒有水平溢位。

修正後再次以同一桌面狀態與來源共同檢視，無剩餘可執行的 P0／P1／P2 問題。手機與深色模式屬來源未提供的延伸狀態。

## 五項視覺檢查

- Fonts/typography：沿用專案 Noto Sans TC 與 sans-serif fallback；標題 700、欄位較常規、次要資訊較小，層級清楚；手機縮小標題與輸入。來源生成圖未指定字體，採用既有字體為合理延伸，未宣稱完全相同的字形或抗鋸齒。
- Spacing/layout：保留左右獎項／數量、垂直分隔、欄上工具列、底部儲存列；桌面 1.42:1，手機上下單欄。勾選欄較來源緊湊以留更多名稱空間，屬接受差異。
- Colors/tokens：白底、灰棕新增／儲存、紅色刪除、淺灰棕勾選行；主色稍深為提高對比。深色模式沿用 ROG 背景與紅色主色，設定標題改亮色確保閱讀。
- Image/assets：設定面板不使用點陣背景；新增及刪除使用官方 Bootstrap Icons SVG，來源及 MIT 授權保留在 assets/icons/LICENSE.txt。原抽獎機圖維持原專案資產。
- Copy/content：標題、編輯中、直接修改欄位、兩欄標題、原咖啡名稱、1／2／3、取消及儲存全部變更均保留；增加復原、驗證與同步回饋。部分全選時顯示 indeterminate，較生成圖的未勾選表頭更準確。

## 功能驗證

- 9 項 Node 測試通過：批次原子套用、無效資料不局部寫入、主持權／畸形請求、舊版本拒絕、回合鎖定、完成解鎖、離線解鎖、舊客戶端相容、邊界驗證。
- 瀏覽器：修改多個獎項、數量 0 顯示錯誤並保留草稿、新增兩欄資料、刪除 2 筆並復原為 5 筆且保留已修改名稱、取消還原。
- 第二分頁加入時草稿仍在；批次儲存後第二分頁的兩個滾輪均收到新的獎項及數量。
- 重複數量顯示機率提示，不自動去重。
- 手機上下兩欄、底部操作可見；深色模式清楚可讀。
- 檢查最終預覽分頁 console error：無錯誤。
- 未執行完整抽獎系統的安全／壓力測試；斷線重連主持權是既有已知限制，非本次已解決範圍。

## Open Questions

無阻擋本次批次編輯的問題。正式發布尚未執行，前後端須共同更新；部署順序詳見 README。

## Implementation Checklist

- [x] 第 2 款設定區版面與手機延伸
- [x] 多行編輯、兩欄新增、勾選刪除、最近一次刪除復原
- [x] 全部儲存與取消草稿
- [x] 前後端驗證與版本檢查
- [x] 多分頁同步驗證及視覺再比對
- [x] 保留所有變更於本機，等待使用者 commit／push 指令

## Follow-up Polish

- [P3] 若未來增加大量獎項，可加入搜尋與鍵盤快速跳列；目前可直接捲動編輯，並以 100 筆上限控制清單。
